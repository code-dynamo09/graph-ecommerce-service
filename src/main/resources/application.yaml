
server:
  port: 8080

spring:
  application:
    name: ecommerce-server
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_gql
    username: root
    password: Mac@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
  graphql:
    websocket:
      path: /graphql

